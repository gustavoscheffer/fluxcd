apiVersion: v1
kind: Service
metadata:
  labels:
    app: flask-app
  name: flask-app
spec:
  ports:
    - name: 5000-tcp
      port: 5000
      protocol: TCP
      targetPort: 5000
      nodePort: 31842
  selector:
    app: flask-app
  type: NodePort
status:
  loadBalancer: {}